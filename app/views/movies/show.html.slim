h4
  i = @movie.title

.row
  .three.columns
    p = image_tag @movie.poster_path

  .nine.columns
    span #{@movie.release_date}  |
    span
      - @movie.genres.each do |g|
        =<> g["name"]
      ' |
    span #{@movie.runtime} mins

    p = @movie.overview